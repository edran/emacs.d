(require 'yasnippet)

;; (add-hook 'prog-mode-hook 'yas-minor-mode 1)

(yas-global-mode 1)

;; (add-hook 'term-mode-hook 
;; 	  (lambda() (yas-minor-mode -1)))

;; (setq yas-trigger-symbol (kbd "C-o"))

;; (add-hook 'org-mode 
;; 	  (lambda() (yas-minor-mode 1)))
